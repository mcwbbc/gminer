- admin_only do
  = render(:partial => "shared/annotation_counts")
= page_title("Samples")
%p
  %form{:name => "sform", :action => "", :style => "display:inline"}
    %label
      Filter on GEO accession or title:
    = text_field_tag(:query, params['query'], :autocomplete => 'off') 
#dataTable
  = render(:partial => "samples_list")
