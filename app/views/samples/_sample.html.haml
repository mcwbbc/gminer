%tr{:class => "#{cycle('row-odd', 'row-even')}"}
  %td{:valign => "top"}
    = link_to(highlight(sample.geo_accession, @q), sample_url(sample))
  %td{:valign => "top"}
    = highlight(sample.series_item.title, @q)
  %td{:valign => "top"}
    = highlight(sample.title, @q)
