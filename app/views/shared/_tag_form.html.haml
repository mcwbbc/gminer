%form#tag-form
  = hidden_field_tag(:authenticity_token, form_authenticity_token)
  = hidden_field_tag(:geo_accession, geo_accession)
  = text_field_tag(:tag_list, '', :size => 30)
  = submit_tag("Add tags", :id => 'add-tags')
