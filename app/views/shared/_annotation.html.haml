%tr{ :class => cycle('rowWhite', 'rowBlue') }
  %td{ :valign => "top", :width => "40%" }
    = ontology_name
  %td{ :valign => "top" }
    - if admin_user?
      = curate_link_list
      = ncbo_ontology_link_list
    - else
      = term_list
