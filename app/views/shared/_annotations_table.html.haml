- admin_only do
  = render(:partial => "shared/admin_nav")
%table{:width => "100%", :border => "0", :cellpadding => "0", :cellspacing => "2"}
  %thead
    %tr.rowHeader
      %td{:width => "20%"}
        %b
          Name
      %td{:width => "80%"}
        %b
          Value
  %tbody#dataTable
    = render(:partial => "shared/field", :collection => item.fields, :locals => {:item => item})
