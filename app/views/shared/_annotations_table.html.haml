- if admin?
  = render(:partial => "shared/admin_nav.html.haml")
%table{:width => "100%", :border => "0", :cellpadding => "0", :cellspacing => "2"}
  %thead
    %tr.row-header
      %td{:width => "20%"}
        %b
          Name
      %td{:width => "80%"}
        %b
          Value
  %tbody.dataTable
    = render(:partial => "shared/field.html.haml", :collection => item.field_array, :locals => {:item => item})
