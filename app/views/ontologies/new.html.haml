- title t('.title')
= page_title(t('.title'))
%br
- semantic_form_for(@ontology) do |f|
  = f.error_messages
  = render(:partial => "ontologies/form", :object => f)
  - f.buttons do
    = f.commit_button t('label.save')
    = f.commit_button t('label.cancel')
